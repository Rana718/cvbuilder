name: Backend CI/CD

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ var.DOCKERHUB_USERNAME }}
          password: ${{ var.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        id: docker_build
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          push: true
          tags: rana718/resume-backend:latest

      - name: Deploy to VPS
        if: steps.docker_build.outputs.pushed == 'true'
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ var.VPS_HOST }}
          username: ${{ var.VPS_USER }}
          password: ${{ var.VPS_PASS }}
          script: |
            cd /home/aydrana/deploymen
            git pull
            echo "Pulling code..."
            cd /home/aydrana/deploymen/cvbuilder/deployment
            chmod +x deploy.sh
            ./deploy.sh
            echo "Deployment script executed."
            
